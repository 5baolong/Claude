# Claude Code 规则

## SAP 代码修改权限规则

修改 SAP ABAP 代码时，严格遵守以下系统权限边界：

| 系统 | 修改权限 |
| --- | --- |
| DEV200 | ✅ 可修改代码（开发系统，所有包） |
| QAS500 | ⚠️ 仅可修改 `$TMP` 包下的对象；其他包禁止 |
| PRD800 | 🚫 永远禁止修改 |

### 执行要求

每次修改 SAP 代码前，先确认当前连接的 connectionId 和目标对象所在包；只要不在允许范围内，一律拒绝修改并提醒用户。

## SAP 代码修改格式规则

修改 SAP ABAP 代码时，**不要删除源代码，也不要直接改原代码**，严格按以下格式操作：

```abap
*===== Changed by WBL <当前日期> =====*
<空行>
*  <被注释掉的旧代码>          "原逻辑：<描述>
<空行>
  <新逻辑代码>                  "新逻辑：<描述>
<空行>
*===== END Changed by WBL <当前日期> =====*
```

- 开始标记：`*===== Changed by WBL <当前日期> =====*`
- 旧代码：整行注释掉（**不删除**），行尾标注 `"原逻辑：...`
- 新逻辑：另起新代码，行尾标注 `"新逻辑：...`
- 结束标记：`*===== END Changed by WBL <当前日期> =====*`

### 何时加 / 不加注释块（以请求是否释放为准）

- 同一段代码、**同一未释放的请求**内反复微调：直接改现有块里的「新逻辑代码」，**不要**反复叠加新的注释块。
- **请求已释放后**再次修改（或修改的是另一段代码）：才新增一个 `===== Changed by WBL <日期> =====*` 块。

## ABAP 新语法优先规则

写 ABAP 代码时，**能用新语法就用新语法**（ABAP 7.40+），避免老式的显式声明：

- **内联声明**：`SELECT ... INTO TABLE @DATA(lt_tab)` 直接用，不要先 `DATA: lt_tab TYPE TABLE OF ...` 定义。
- **构造/内表表达式**：优先 `VALUE #( )`、`CORRESPONDING #( )`、`COND #( )`、`table[ key ]`、`FOR`。
- **只在需要时才显式声明**：变量要复用、类型要特殊指定、或跨作用域可见时才 `DATA:` 显式定义。

```abap
"推荐：内联声明，不用定义内表
SELECT saknr, txt50 FROM skat INTO TABLE @DATA(lt_skat) WHERE ...

"不推荐：先显式定义内表
DATA: lt_skat TYPE TABLE OF skat.
SELECT saknr, txt50 FROM skat INTO CORRESPONDING FIELDS OF TABLE @lt_skat WHERE ...
```

## Claude Code 行为规则（ABAP 适配版）

> 来源：网上流传的 Karpathy 4 条 → 12 条社区规则，精选其中贴合 ABAP 开发的 8 条，与上面权限/格式规则配合使用。
> 原「外科手术式修改」已被上方格式规则覆盖、不重复；「模型只用于判断」「Token 预算」「模式冲突二选一」对纯 ABAP 开发贴合度低，已剔除。
> 判据：每条都要能回答「它防的是哪种错」；防不住实际坑的规则不写。

### 1. 先想后写：说清假设，不默默猜测

- 接到「改下这个报表/程序」时，先确认业务逻辑前提，不要默认表里一定有数据、选择屏幕字段一定非空。
- 有取舍（改读取方式 vs 兼容旧逻辑、加缓存 vs 直读 DB）时摊开讲，别自己拍板。
- 不确定用哪个对象/表/事务代码就问，别拿猜的名字去写 SELECT 或调用。
- 动手前先用一两句话复述你要改什么、为什么改，让用户确认没理解偏。

### 2. 简单优先：最小改动，不顺手加功能

- 写能解决当前问题的最小代码。一次性 ALV 报表就复用项目现用模式（该用 REUSE_ALV 就用），不要套 OO/框架。
- 不写"以后可能用得上"的增强、参数、报表、按钮——需求里没有的不加。
- 不为一次性代码建抽象/工厂；能用标准 FM/BAPI 就优先用。

### 3. 写前先读：动代码前先读对象本身

- 调用任何 FM / 类方法前，先读它的**真实签名**（训练数据里的签名经常是错的，猜的参数必然运行时报错）。
- 写 SQL 前先确认表和字段在 DDIC 里真实存在、键字段正确、`WHERE` 条件不会扫全表。
- 改共用 include / 方法前，先看它的调用方，确认改动不会波及其他程序。
- 对拿不准的对象用 ADT 查结构 / 使用处（Where Used），别凭印象。

### 4. 目标驱动：讲"成功长什么样"，不喂步骤

- 提需求时给出可验证的成功标准，例：程序跑完无 dump、`sy-subrc = 0`、目标表行数/金额变化符合预期。
- 循环「改 → 验证」直到达标，而不是改完一遍就当完成。
- 每处改动明确自查口径：语法检查、激活、跑真实场景、核对数据，缺一不算完。

### 5. 验证意图：语法过了 ≠ 改对了

- 语法检查、ATC 通过只是最低门槛；真正要验证的是「业务逻辑若变化，检查会失败」。
- 适合的地方写 ABAP Unit（LTCL 测试类）；不适合的，改动后跑一次真实场景并核对数据（行数、金额、键值、单据状态）。
- 一个"跑不跑都通过"的检查等于没有检查，别拿"看起来没问题"糊弄。

### 6. 多步任务要 checkpoint：每步说清状态再往下

- 涉及「改动 → 激活 → 传请求 → QAS 验证」多环节时，每完成一环明确汇报：做了什么、语法/数据是否已验证、还剩什么、当前请求号。
- 说不清自己改到哪一步时先停下来，不要继续堆改。
- 中途清上下文 / 跨 session 前，先留下清晰的进度记录（对象、行号、改了什么、未验证什么）。

### 7. 惯例优先：先跟旧代码，再谈风格

- 目标对象是固定格式 / 大写 / `*` 注释的老代码，就按它的风格改，不要顺手"现代化"重构。
- 代码库是 Clean ABAP 风格就按 Clean 写；新旧混用时不要自己发明第三种风格。
- 不认同某个惯例可以单独提出来讨论，但别在本次改动里单方面分叉。

### 8. 失败要大声：报行数，不报"成功"

- `MODIFY` / `DELETE` / `COMMIT` 后必须检查 `sy-subrc` 与实际影响行数；批量处理里跳过/失败的明细要报出来。
- 调 BAPI / FM 后扫返回消息里的 E 类型（如 `BAPIRET2`），有错误就原样呈现，不要吞掉。
- 只有没有任何跳过/错误时才能说"全部成功"；验证跳过任何一项就是没通过。
- 主动暴露不确定点（某表数据可能为空、依赖先后台任务等），而不是事后才发现。
